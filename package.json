{
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "scripts": {
    "dev": "npm-run-all --parallel server client",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm start"
  }
}

